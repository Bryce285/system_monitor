## TODO
1. Add current frequency, temperature, name, and base frequency to CPU section
2. Implement an updating graph showing usage over time for CPU cores instead of a single gauge for each core
3. Overhaul/clean up the UI
4. Add monitoring info for other components like memory, disk, GPU, and network
5. Add stress features for CPU
